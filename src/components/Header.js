import React from 'react';

export const Header = () => {
    return(
        <div>
            <span>This website displays all users on the left and users' details on the right</span>
        </div>
    );
};